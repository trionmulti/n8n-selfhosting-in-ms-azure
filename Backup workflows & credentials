Backup workflows & credentials
tar -czvf n8n_backup.tar.gz n8n_data/

Restore
tar -xzvf n8n_backup.tar.gz

🟪 11. Security Checklist

✔ Enable Basic Auth
✔ Use HTTPS via Caddy
✔ Change admin password regularly
✔ VM firewall limited to:

80 (Caddy)

443 (Caddy)

SSH (22)
✔ Keep OS updated:

sudo apt update && sudo apt upgrade -y
